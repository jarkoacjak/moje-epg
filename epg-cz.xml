name: Aktualizácia EPG súboru

on:
  schedule:
    - cron: '0 2 * * *'  # Spustí sa každú noc o 02:00 UTC
  workflow_dispatch:     # Pridá tlačidlo na ručné spustenie

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Stiahnutie repozitára
      uses: actions/checkout@v3

    - name: Nastavenie Pythonu
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Inštalácia závislostí
      run: |
        pip install requests beautifulsoup4

    - name: Spustenie generátora EPG
      run: python generator.py

    - name: Nahranie zmeneného XML súboru späť na GitHub
      run: |
        git config --global user.name "EPG Bot"
        git config --global user.email "bot@github.com"
        git add epg-cz.xml
        git commit -m "Automatická aktualizácia EPG" || exit 0
        git push


